#pragma once
namespace mcy
{
  class mcy
  {
  public:
    // Returns void remove arr one index 
    static void removeElementAtArry ( int *arr, int index, int size );

    // Returns a * b
    //static double Multiply(double a, double b);

    // Returns a / b
    //static double Divide(double a, double b);
  };
}